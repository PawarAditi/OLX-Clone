  

   
     
        
        <!Doctype html>
        
        <html>
        
        <head>
            <title>List of Ads</title>
        </head>
        
        <body> 
            
            <h1>List of Ads</h1>
            
                <div class="row">
                    
                    <% if(taskData.length> 0 )
                        {
                        taskData.forEach(function (data){
                        %>
                        
                            <% var imgUrl = "/image/"+data._id+"."+data.image%>
                            <div class="column">
                                <img style="width:200px;height:200px;" src=<%=imgUrl%>><br>
                                Ad Name : <%= data.name %><br> 
                                Description : <%= data.description %> <br>
                                Price : <%= data.price %><br>
                                Time Created :  <% var dObj=new Date(data.adDateTime) 
                                var date=dObj.getDate()+"/"+(dObj.getMonth()+1)+"/"+dObj.getFullYear()+" "+ dObj.getHours()+":"+dObj.getMinutes()+":"+dObj.getSeconds() %>
                                    <%=date%><br>
                                <a href="/member/updatecontent/<%=data._id%>">Update Content</a>
                                <a href="/member/reuploadImg/<%=data._id%>">Update Image</a>
                                <a href="/member/deletead/<%=data._id%>">Delete Ad</a>
                            
                            </div> 
                        <% }) } %>
                    </div>
                
        
        </body>
        <style>
             body {
    background: linear-gradient(90deg, #C7C5F4, #776BCC);		
}
.row {
  display: flex;
}

/* Create three equal columns that sits next to each other */
.column {
  flex: 33.33%;
  padding: 5px;
}
img {
  border: 1px solid white; /* Gray border */
  border-radius: 4px;  /* Rounded border */
  padding: 5px; /* Some padding */
  width: 150px; /* Set a small width */
}
        #outer
{
    width:100%;
    text-align: center;
    
}
.login__submit {
	background: #fff;
	font-size: 14px;
	margin-top: 30px;
	padding: 16px 20px;
	border-radius: 26px;
	border: 1px solid #D4D3E8;
	text-transform: uppercase;
	font-weight: 700;
	display: flex;
	align-items: center;
	width: 120%;
	color: #4C489D;
	box-shadow: 0px 2px 2px #5C5696;
	cursor: pointer;
	transition: .2s;
    position: absolute;
  
  left: 0;
  width: 120px;
  transform: translateY(-40%);
  transform: translateX(-189%);

}
.inner
{
    display: inline-block;
    align-content: center;
    text-align: center;
    margin-left: auto;
    margin-right: auto;
}
h1{
            align-content: center;
            text-align: center;
        }
        </style>
        
        </html>
       